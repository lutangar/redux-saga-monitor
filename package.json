{
  "name": "redux-saga-monitor",
  "version": "0.12.0",
  "description": "Dispatch monitoring events for redux saga",
  "main": "./lib/index.js",
  "jsnext:main": "es/index.js",
  "scripts": {
    "lint": "eslint src",
    "test": "cross-env BABEL_ENV=cjs babel-node test/index.js | tap-spec",
    "check": "npm run lint && npm run test",
    "compile": "rimraf lib && cross-env BABEL_ENV=cjs babel -d lib/ src/",
    "build:umd:dev": "cross-env BABEL_ENV=cjs webpack src/index.js dist/redux-saga-monitor.js --config webpack.config.dev.js",
    "build:umd:prod": "cross-env BABEL_ENV=cjs webpack src/index.js dist/redux-saga-monitor.min.js --config webpack.config.prod.js",
    "build:es": "cross-env BABEL_ENV=es babel src --out-dir es",
    "build": "rimraf dist es && npm run build:umd:dev && npm run build:umd:prod && npm run build:es",
    "prepublish": "npm run check && npm run compile && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lutangar/redux-saga-monitor.git"
  },
  "authors": [
    "Yassine ELOUAFI <yelouafi@gmail.com>",
    "John Babak",
    "Romain Séguy",
    "Johan Dufour <johan.dufour@gmail.com>"
  ],
  "keywords": [
    "redux",
    "redux-saga",
    "middleware",
    "saga",
    "logger",
    "monitor"
  ],
  "license": "MIT",
  "dependencies": {
    "cross-env": "^3.1.2",
    "is-primitive": "^2.0.0",
    "redux-saga": "^0.12.0",
    "util": "^0.10.3"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-core": "^6.4.0",
    "babel-eslint": "^6.1.2",
    "babel-loader": "^6.2.1",
    "babel-preset-es2015": "^6.14.0",
    "eslint": "^3.4.0",
    "eslint-loader": "^1.2.0",
    "rimraf": "^2.5.4",
    "tap-spec": "^4.1.1",
    "tape": "^4.2.2",
    "webpack": "1.13.0"
  }
}
